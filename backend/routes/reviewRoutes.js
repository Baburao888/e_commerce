// routes/reviewRoutes.js
// Note: review endpoints are actually registered as nested routes inside
// productRoutes.js (POST/GET /api/products/:id/reviews) since a review
// always belongs to a product. This file is kept for clarity/consistency
// with the required folder structure, and simply re-exports for reference.

module.exports = {};
